"""
SafeNet AI — JWT Authentication Routes
TODO (Backend Engineer — Task 2.1): implement this file.
See WORK_REMAINING.md for exact step-by-step instructions.

Endpoints to build:
  POST /api/v1/auth/register
  POST /api/v1/auth/login
  POST /api/v1/auth/refresh
  GET  /api/v1/auth/me

All schemas already exist in backend/schemas/schemas.py
User model already exists in backend/db/models.py
"""

from fastapi import APIRouter

router = APIRouter(prefix="/auth", tags=["Authentication"])

# TODO: implement routes
# See WORK_REMAINING.md → Task 2.1
